using UnityEngine;

/// <summary>
/// Handles UI presentation and user interface state.
/// Displays information received from the game state without owning gameplay logic.
/// </summary>
public class UIView : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
