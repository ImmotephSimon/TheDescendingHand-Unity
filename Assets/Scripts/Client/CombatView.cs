using UnityEngine;

/// <summary>
/// Handles the visual representation of combat events.
/// Spawns combat-related visuals, audio, and feedback based on server results.
/// Does not calculate or resolve combat outcomes.
/// </summary>
public class CombatView : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
